{
  "name": "neural-drive-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Optional backend API for Neural Drive Lab",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
